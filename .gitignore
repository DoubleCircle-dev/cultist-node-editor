node_modules
.vscode-test/
*.vsix
.env

.vscode/settings.json

# Agent 临时工作区：优化方案文档与 agent 运行时产生的临时脚本/调试文件
# （不进版本库，避免污染仓库）
agent-scratch/

# origin 图片资源（~233MB / 2129 张）已拆到独立仓库经 CDN 托管：
#   仓库 https://github.com/DoubleCircle-dev/cultist-node-editor-assets
#   引用 https://cdn.jsdelivr.net/gh/DoubleCircle-dev/cultist-node-editor-assets@v1/
# 本地仍保留该目录；名字→路径映射见 core/origin_resources/image-index.json
core/origin_resources/images/

# origin 中间态快照（~34MB）：由 `pnpm run gen:origin-snapshot` 生成，
# 打包（package:vsix）前会自动跑一次并随之进入 VSIX，但不进版本库
core/origin_resources/origin.graph.json
